% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ranking_quiz.R
\name{ranking_quiz_questions}
\alias{ranking_quiz_questions}
\title{Generate Ranking Quiz Questions}
\usage{
ranking_quiz_questions(
  tags = NULL,
  items_per_question = 3L,
  n_questions = 5L,
  seed = NULL,
  difficulty = NULL,
  profile = list()
)
}
\arguments{
\item{tags}{Character vector of tags to include (e.g. "Radiation",
"Travel"). Use \code{NULL} for all tags. See \code{\link[=ranking_tag_mapping]{ranking_tag_mapping()}} for
available tags.}

\item{items_per_question}{Integer. Number of items per question (2, 3, or 4).
Default 3.}

\item{n_questions}{Integer. Number of questions to generate. Default 5.}

\item{seed}{Optional integer seed for reproducibility.}

\item{difficulty}{Optional difficulty level: "easy", "medium", "hard",
or "mixed". Easy = large LLE spread within question, hard = small spread.}

\item{profile}{A named list of condition variables for filtering conditional
risks, passed to \code{\link[=common_risks]{common_risks()}}. E.g. \code{list(country = "NG")} to include
Nigerian disease mortality in the acute risk pool. Default \code{list()}.}
}
\value{
A tibble with columns:
\itemize{
\item \code{question_id}, \code{tag}, \code{item_name}, \code{item_source} ("acute"/"chronic"),
\code{lle_minutes}, \code{micromorts}, \code{microlives_per_day}, \code{category},
\code{description}, \code{help_url}, \code{correct_rank}, \code{difficulty}
}
}
\description{
Creates questions for the ranking quiz by combining acute (micromort)
and chronic (microlife) risks, converting to a common Loss of Life
Expectancy (LLE) scale, and grouping into rankable sets.
}
\examples{
ranking_quiz_questions(tags = "Travel", n_questions = 3, seed = 42)

}
