% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk_equivalence.R
\name{risk_equivalence}
\alias{risk_equivalence}
\title{Risk Equivalence Table}
\usage{
risk_equivalence(reference, risks = NULL, min_ratio = 0.01, max_ratio = Inf)
}
\arguments{
\item{reference}{Character. Activity name to use as the reference
(denominator). Must match an \code{activity} value in \code{risks}.}

\item{risks}{A tibble with at least \code{activity} and \code{micromorts} columns.
Defaults to \code{\link[=common_risks]{common_risks()}}.}

\item{min_ratio}{Numeric. Minimum ratio to include (default 0.01).}

\item{max_ratio}{Numeric. Maximum ratio to include (default \code{Inf}).}
}
\value{
A tibble with columns: \code{activity}, \code{micromorts}, \code{reference},
\code{reference_micromorts}, \code{ratio}, \code{equivalence}.
}
\description{
Compares a reference activity to all other activities by computing the
ratio of micromorts. "How many X-rays equal one skydive?"
}
\examples{
risk_equivalence("Chest X-ray (radiation)")
risk_equivalence("Skydiving (per jump, US)")
}
\seealso{
\code{\link[=risk_exchange_matrix]{risk_exchange_matrix()}}
}
