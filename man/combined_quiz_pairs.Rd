% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combined_quiz.R
\name{combined_quiz_pairs}
\alias{combined_quiz_pairs}
\title{Generate cross-domain quiz pairs comparing acute and chronic risks}
\usage{
combined_quiz_pairs(n = 10, time_period_days = 365, seed = NULL)
}
\arguments{
\item{n}{Integer. Number of pairs to generate. Default \code{10}.}

\item{time_period_days}{Numeric. Time period for chronic risk accumulation.
Default \code{365} (1 year). For chronic risks, the cumulative impact is
\verb{abs(microlives_per_day) × time_period_days}.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility. Default
\code{NULL} (random each call).}
}
\value{
A tibble with columns:
\itemize{
\item \code{activity_a}, \code{type_a} ("acute"), \code{value_a}, \code{unit_a} ("micromorts"),
\code{category_a}, \code{period_a}
\item \code{factor_b}, \code{type_b} ("chronic"), \code{value_b}, \code{unit_b} ("microlives/day"),
\code{category_b}, \code{direction_b}
\item \code{common_unit} ("microlives"), \code{common_value_a}, \code{common_value_b}
\item \code{correct_answer} ("a" or "b" — whichever has higher impact in common
unit)
\item \code{ratio} (larger / smaller common value)
\item \code{explanation} describing both values and the conversion
}
}
\description{
Creates candidate question pairs mixing acute risks from \code{\link[=common_risks]{common_risks()}}
(micromorts per event) with chronic risks from \code{\link[=chronic_risks]{chronic_risks()}} (microlives
per day), converting both to a common microlife scale so they can be
directly compared. Each pair pits one acute activity against one chronic
lifestyle factor and asks which has a larger total impact over
\code{time_period_days}.
}
\details{
The conversion from micromorts to microlives uses the factor
\code{0.7} (1 micromort ≈ 0.7 microlives at age 40). Chronic impact over the
time period is \verb{abs(microlives_per_day) × time_period_days}.
}
\examples{
pairs <- combined_quiz_pairs(n = 5, seed = 42)
pairs[, c("activity_a", "factor_b", "correct_answer", "ratio")]

# One year time period (default)
pairs_year <- combined_quiz_pairs(n = 10, time_period_days = 365, seed = 1)

}
