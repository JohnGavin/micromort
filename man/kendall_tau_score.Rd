% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ranking_quiz.R
\name{kendall_tau_score}
\alias{kendall_tau_score}
\title{Kendall Tau Score for Ranking Quiz}
\usage{
kendall_tau_score(user_order, correct_order)
}
\arguments{
\item{user_order}{Integer vector of item indices in the user's order
(first element = user's #1 pick, i.e. most risky).}

\item{correct_order}{Integer vector of item indices in the correct order.}
}
\value{
A list with:
\itemize{
\item \code{score}: number of concordant pairs
\item \code{max_score}: total number of pairs = k*(k-1)/2
\item \code{n_concordant}: same as score
\item \code{n_discordant}: pairs where user and correct order disagree
\item \code{pct}: percentage score (0-100)
}
}
\description{
Computes how well a user's ranking matches the correct ranking using
the Kendall tau distance. Counts concordant pairs (user agrees with
correct order) vs discordant pairs (user disagrees).
}
\examples{
# Perfect score
kendall_tau_score(c(1, 2, 3), c(1, 2, 3))

# Completely reversed
kendall_tau_score(c(3, 2, 1), c(1, 2, 3))

# One swap
kendall_tau_score(c(2, 1, 3), c(1, 2, 3))

}
