% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk_sensitivity.R
\name{risk_sensitivity}
\alias{risk_sensitivity}
\title{Sensitivity Analysis for Activity Risk Estimates}
\usage{
risk_sensitivity(activity = NULL, pct = 20)
}
\arguments{
\item{activity}{Character scalar — activity name matching a row in
\code{\link[=common_risks]{common_risks()}}. Pass \code{NULL} (default) to return sensitivity for all
activities.}

\item{pct}{Numeric scalar — percentage variation applied symmetrically
around the base estimate. Default \code{20} (i.e., ±20\%). Must be in (0, 100).}
}
\value{
A tibble with columns:
\describe{
\item{activity}{Activity name}
\item{micromorts_base}{Base micromort estimate from \code{\link[=common_risks]{common_risks()}}}
\item{micromorts_low}{Low estimate: \code{base * (1 - pct/100)}}
\item{micromorts_high}{High estimate: \code{base * (1 + pct/100)}}
\item{rank_base}{Rank of the activity at the base estimate (1 = highest risk)}
\item{rank_change}{Absolute rank positions shifted between low and high estimates}
}
}
\description{
Computes how activity micromort rankings shift when the base estimate is
varied by ±\code{pct}\%. Useful for communicating uncertainty around point
estimates derived from sparse epidemiological data.
}
\details{
Activities are sourced from \code{\link[=common_risks]{common_risks()}}. The \code{rank_change} column
reports the absolute number of ranking positions an activity moves between
its low and high estimate when all activities are re-ranked.
}
\examples{
# Sensitivity for a single activity
risk_sensitivity("Skydiving (US)")

# Sensitivity for all activities at ±10\%
risk_sensitivity(pct = 10)

# Activities with the largest rank uncertainty
risk_sensitivity() |> dplyr::arrange(dplyr::desc(rank_change))
}
\seealso{
\code{\link[=common_risks]{common_risks()}}, \code{\link[=daily_hazard_rate]{daily_hazard_rate()}}

Other analysis: 
\code{\link{annual_risk_budget}()},
\code{\link{compare_interventions}()},
\code{\link{daily_hazard_rate}()},
\code{\link{lifestyle_tradeoff}()},
\code{\link{toxicological_risk}()}
}
\concept{analysis}
