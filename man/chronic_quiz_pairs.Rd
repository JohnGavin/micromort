% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quiz.R
\name{chronic_quiz_pairs}
\alias{chronic_quiz_pairs}
\title{Generate Quiz Pairs for "Which Daily Habit Has a Bigger Effect?" Game}
\usage{
chronic_quiz_pairs(
  min_ratio = 1.1,
  max_ratio = 2,
  prefer_cross_category = TRUE,
  seed = NULL,
  difficulty = NULL
)
}
\arguments{
\item{min_ratio}{Minimum ratio between absolute microlife values in a pair.
Default 1.1. Ignored when \code{difficulty} is non-NULL.}

\item{max_ratio}{Maximum ratio. Default 2.0. Ignored when \code{difficulty} is
non-NULL.}

\item{prefer_cross_category}{If \code{TRUE} (default), pairs from different
categories are prioritised.}

\item{seed}{Optional random seed for reproducibility.}

\item{difficulty}{Optional difficulty level. One of \code{"easy"}, \code{"medium"},
\code{"hard"}, or \code{"mixed"}. When non-NULL, overrides \code{min_ratio}/\code{max_ratio}
to use the full pool (ratios 1.5--10) and assigns difficulty via
data-driven terciles.}
}
\value{
A tibble with columns:
\itemize{
\item \code{factor_a}, \code{microlives_a}, \code{direction_a}, \code{category_a},
\code{description_a}, \code{help_url_a}, \code{annual_days_a}
\item \code{factor_b}, \code{microlives_b}, \code{direction_b}, \code{category_b},
\code{description_b}, \code{help_url_b}, \code{annual_days_b}
\item \code{ratio} (max/min of absolute microlife values)
\item \code{answer} ("a" or "b" -- whichever factor has the larger absolute effect)
\item \code{difficulty} (only when \code{difficulty} is non-NULL)
}
}
\description{
Creates candidate question pairs from \code{\link[=chronic_risks]{chronic_risks()}} for use in an
interactive microlife comparison quiz. Each pair contains two chronic
lifestyle factors, and the player guesses which has the larger absolute
effect on life expectancy (in microlives per day).
}
\examples{
pairs <- chronic_quiz_pairs(seed = 42)
head(pairs)

easy <- chronic_quiz_pairs(difficulty = "easy", seed = 42)
head(easy)

}
